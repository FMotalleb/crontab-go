# yaml-language-server: $schema=https://raw.githubusercontent.com/fmotalleb/crontab-go/main/schema.json

# OpenTelemetry observability (optional).
# Can also be configured via env vars: OTEL_TRACING_URL, OTEL_METRICS_URL, OTEL_LOG_URL, etc.
# observability:
#   service-name: "crontab-go"
#   # Extra resource attributes attached to every trace, metric, and log (OTLP log record) emitted.
#   attributes:
#     env: "production"
#     region: "eu-west-1"
#   tracing:
#     url: "http://collector:4318"
#     insecure: false
#   metrics:
#     url: "http://collector:4318"
#     insecure: false
#     interval: 60s
#   log:
#     url: "http://collector:4318"
#     insecure: false

jobs:
  - name: Test Job
    description: Sample of a single job
    tasks:
      - command: echo $(whoami)
        retries: 3
        retry-delay: 1s
        timeout: 15s
        user: root
    events:
      - on-init: true
      - cron: "@yearly"
      - interval: 10m10s
    hooks:
      done:
        - command: echo Ok
      failed:
        - command: echo Failed
