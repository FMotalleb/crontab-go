# yaml-language-server: $schema=schema.json

jobs:
  - name: echo
    tasks:
      - command: echo "{{ . | toJSON }}"
        env:
          SHELL: /bin/bash
          COLE: test
    events:
      # - cron: "0 * * * * *"
      - interval: 5s
      - on-init: true
      # - web-event: test
      # - docker:
      #     image: nginx
